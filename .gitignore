__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
dist/
build/
*.egg-info/

.cut4-acceptance/
.cut8-acceptance/
node_modules/
.cut9-acceptance/
.cut10-acceptance/
.cut11-acceptance/
.cut14-acceptance/
.cut*-acceptance/
.lifecycle-wrappers-test/

# work roots: acceptance runs, reproduction corpora, caches. One hidden dir
# beside the main checkout on the certified volume, so a new variant needs no
# .gitignore edit and the project root stays legible (beliefs-5aad8c).
.work/
# Fallback test-timing log, written by tools/tt when the shared log is unwritable and
# emptied by ops `tt-report`; never committed.
.tt/
