# Writer-session execution ledger (cut 19, 2026-09-05/06)

The subagent-driven execution record of `2026-09-05-writer-session.md`: pre-flight scan, per-task completions, fix rounds, every ruling the controller made, and the deferred minors the final review triaged. Kept as a durable artifact; the code history is the record of what landed.


Spec: docs/designs/2026-09-05-writer-session-design.md (§7, §9 frozen); cut record docs/designs/2026-09-05-conformance-cut-19.md frozen at 5cc2153.
Branch feat/writer-session, worktree .worktrees/writer-session. Start HEAD 352dba4.
Task children: T1 beliefs-6ea04b, T2 beliefs-6c5383, T3 beliefs-57ab69, T4 beliefs-d380c4, T5 beliefs-e9a564, T6 beliefs-5032af, T7 beliefs-285935, T8 beliefs-e1f6a4, T9 beliefs-f47169, T10 beliefs-430a2d, T11 beliefs-7be34d, T12 beliefs-bb1cb3.

## Pre-flight scan
| pair / task | produces vs consumes | found |
|---|---|---|
| T2→T3/T5 | errors.PlanRefused, OperationPortMissing; OPERATION_KINDS corpus-write | consistent |
| T3→T4 | durable_executor_factory() instance with recover(root) ← _RootState.recover | consistent |
| T3→T5 | preflight(plan), execute_fulfilling -> str ← commit_fulfilling | consistent |
| T4→T5 | _Fulfillment, _RoutedExecutor, _RootState.fulfilling ← _fulfilling/_run | T4 Interfaces block said `_Fulfillment(authority, port, consumed, result)` and `_RoutedExecutor(inner, state)`; body (line ~963, ~990) has `submitted` field and `holder` param. |
| T5→T7 | CorpusWriter.operations, OperationCommit ← ScopedWriter | consistent |
| T6→T7/T8 | LedgerWriter/LedgerReader/read_ledger_evidence | consistent |
| T7→T8 | session/__init__ open runs no reconciliation until T8; findings=() | consistent |
| T7/T8→T9/T10 | open_attended_session, reconcile_sessions, test names | consistent |
| T9/T10→T11 | check names cited by arms incl. J2h/J1g | consistent |
| T11→T12 | runner output → results record | consistent |
| each task self | tests vs code, files created vs later touched | no contradictions seen; ordinary-body freeze + stale: [] consistent across tasks |
| T1 science repo | commit in the sibling science repository (clean, main) — side effect outside worktree, explicitly in the reviewed plan | proceed |

Ruling: T4 Interfaces block corrected in place to `_Fulfillment(authority, port, consumed=False, submitted=False, result=None)` and `_RoutedExecutor(inner, holder)` — the body is the plan's argument and matches §13 item 11 — cost if wrong: none beyond prose.
Ruling: the science-repo docs commit in Task 1 proceeds without asking — it is a local docs commit on that repo's main, spelled out in the plan the user reviewed three times and chose to execute — cost if wrong: one revertable commit.

## Progress
T1 dispatched (implementer sonnet), BASE e537334
T1 round 0: implementer BLOCKED — the plan's staleness probe crashed (cut 6 arms name `world.py`, a module promoted to a package at f703913) and the tree already carries 24 stale entries in cuts 5, 6, 8, 10.
Ruling: the probe gains an existence guard (reports "missing") and the baseline is the verbatim line it prints on e537334 (saved in stale-baseline.txt, banked in §13 item 5); the requirement becomes "output identical to baseline", not `stale: []` — the prefix runner executes only cuts 17 and 18, which have zero entries; earlier entries are invalidated frozen evidence under the cited-not-run precedent (freeze-after-review-supersede-by-citation) — cost if wrong: a new stale arm in cuts 5/6/8/10 could hide among existing ones, but the equality check catches any addition.
Ruling: gate command loses its extra -q (addopts already sets it; doubling hides the count) — plan and constraints file patched — cost if wrong: none.
T1 implementer DONE at 7386093 (science 9a4dd9c); full suite 3517 passed in 15m42s; reviewer (sonnet) dispatched
Task 1: review clean; ⚠️ resolved by controller: 7386093 trailer has its blank line (git log -1 --format=%B); task record went through the CLI (tasks check clean, report says CLI).
Task 1: minor (deferred): science §4.2 paragraph re-wrapped cosmetically by the insertion.
Task 1: complete (commits e537334..7386093, review clean)
T2 dispatched (implementer sonnet), BASE af3ead8
T2: implementer's final report never arrived (session paused); user reports Task 2 committed as 7381e28 and main merged as d2dc6af; tree clean, main is ancestor; probe == baseline after the merge. Task record beliefs-6c5383 was left `doing` — closed by controller in a chore commit.
Ruling: Task 2's task review runs now, read-only, concurrently with Task 3's implementer; any findings are queued for a fix dispatch after Task 3 lands (no two writers at once) — cost if wrong: Task 3 builds briefly on an unreviewed Task 2 (small, additive diff).
User instructions on resume (2026-09-05): (a) roadmap tier 1 now ranks by distance to the mm30 dogfood (design §4.0 amended 2026-09-05); Task 12's re-rank: Ranked at → cut 19, writer-session leaves roadmap and ledger, verification-publication becomes the write-path lane's next boundary; never restore breadth ordering. (b) ledger writer-session row and roadmap placement cell already say cut 19 frozen — Task 12 removes the row, not edits it. (c) a reproduction lane may open from main (concurrency rule 6); it edits no kernel surface; findings arrive as task records addressed to this lane. (d) no further design/plan review round before the discharge results record; cut 19 stays frozen at 5cc2153 with cut18_acceptance.py prefix. (e) no attribution trailers from now on; never commit incidental python/uv.lock changes.
T2 reviewer (sonnet) dispatched on af3ead8..7381e28; T3 dispatched (implementer sonnet), BASE 16df2c6
Task 2: review clean; ⚠️ (wiring of the error types is later tasks' scope) — not a gap. Minor: task record closed in follow-up commit 16df2c6 rather than atomically.
Task 2: complete (commits af3ead8..7381e28 + 16df2c6, review clean)
T3 implementer DONE at cdf2a63 (3539 passed, 17m42s); 3 judgment calls flagged for review; reviewer (opus) dispatched
Task 3: review clean (opus). minor (deferred): test_run_persistence.py KilledAfterAppend ports stub preflight with raise instead of pass; _log_seam/log_seam duplicate names in root.py; chain_head_reader docstring says "function object" for the factory instance; _registration_for's two failure branches and recover's chain-read failure branch untested; test_recover_on_a_registered_root_reads_the_chain observes nothing about the read; readback failure after commit surfaces as ExecutionError(applied=None) (plan-mandated, keep in view for Task 5/10); four certified_work tests sit in tests/test_operation_port.py not tests/acceptance (brief governs).
Task 3: complete (commits 16df2c6..cdf2a63, review clean)
T4 dispatched (implementer opus), BASE cdf2a63
T4 implementer DONE_WITH_CONCERNS at cc3e5a3 (3546 passed; 6 pre-existing identity assertions redirected through ._lock/._inner; @final on _SettlingHold); reviewer (opus) dispatched
Task 4: review clean (opus). minor (deferred): depth never exercised above 1 (outermost-hold branch untested; plan-mandated test list) — Task 5 to add a nesting arm if it introduces nesting; a swallowed inner-hold exception would clear unresolved (plan semantics, unreachable today); _RootState carries executor_factory and executors without a comment on why both; test_root.py cast() instead of type assertion; scope.consumed set before the permit check (in-memory bookkeeping, plan-mandated); _settle's lock-held precondition only in prose.
Task 4: complete (commits cdf2a63..cc3e5a3, review clean)
T5 dispatched (implementer opus), BASE cc3e5a3
T5 implementer DONE_WITH_CONCERNS at 5951f35 (3570 passed; added 37th E1 case in test_permit_entry_points.py; two test-text fixes); reviewer (opus) dispatched
Task 5: review (opus): 1 Important (plan-mandated): intent wire encoding duplicated at corpus.py:476 and :1673. minor (deferred): commit_fulfilling does not validate the port's returned digests (adjacent seam does); mint_coordination/revise_coordination twins have no behavioral test; "unresolved after the preflight" half of §13 item 12 unpinned; test_a_permit_refusal_appends_nothing shares one tmp_path across params (harmless today); test_commit_fulfilling_requires_the_kinds_the_plan_emits tests _plan_kinds only; _fulfilling's raw-lock precondition documentary only.
Ruling: the duplication finding stands against the plan text — no arm pins the body of `_append_operation_intent` (arms cite only its callers), so a shared module-level encoder used by both sites is safe; fix round 1 resumes the implementer — cost if wrong: a stale arm, which the probe would reveal before commit.
Task 5: fix round 1/5 (1 addressed, 0 open — shared _encode_operation_intent used by both producers, test compares both real payloads; commits 5951f35..9cffbcc)
Task 5: complete (commits cc3e5a3..9cffbcc, review clean)
T6 dispatched (implementer sonnet), BASE 9cffbcc
T6 implementer DONE_WITH_CONCERNS at 26c2e45 (3591 passed; from copy import deepcopy for the capability-boundary allowlist; two test_boundary.py tests flaked once then passed); reviewer (sonnet) dispatched
Task 6: review (sonnet): 1 Important (plan-mandated): LedgerReader silently tolerates an invocation-close for an unopened id, an act for an unopened invocation (kept as _stray_acts), and an act after its invocation-close. minor (deferred): best-effort close() suppression in append's failure path lacks a comment; stray acts lose file order (moot after the fix).
Ruling: the finding stands against the plan text — the design (§7 J7 "the reader refuses a malformed line", §6 "every ledger state, malformed included, is a finding") and the fail-early rule make ledger content the writer's protocol cannot produce malformed, not evidence to salvage; the reader raises LedgerMalformed naming the line for: close of an unopened id, act for an unopened id, act after close, a second open of the same id, and any line after session-close; _stray_acts is removed — cost if wrong: a corrupt ledger is classified session-ledger-malformed as a whole rather than partially reconciled, which the design already lists as a finding.
Task 6: fix round 1/5 (1 addressed, 0 open — reader refuses the five protocol-impossible sequences, _stray_acts gone; commits 26c2e45..9c75761)
Task 6: minor (deferred): a second invocation-close of a closed id is refused but untested; a second session-open after line 1 is still silently accepted.
Task 6: complete (commits 9cffbcc..9c75761, review clean)
T7 dispatched (implementer opus), BASE 9c75761
T7 implementer DONE_WITH_CONCERNS at 1a49a38 (3616 passed). Concern: Authority(...) may be constructed only in permit.py (write-permits §16 arm) → added permit.scoped_authority(required, actor) and design §13 item 17.
Ruling: scoped_authority in permit.py is the right seam — the design's own rule forbids constructing Authority elsewhere, the helper adds no capability and takes the actor the session derives; §13 item 17 is the sanctioned place to record it — cost if wrong: one small public helper in permit.py. Reviewer (opus) dispatched.
Task 7: review (opus): 1 Important (plan-mandated): currency can change between _require_current and _record_act (claim/close take only the session lock, _act takes only the root lock) → a durable commit droppable from the ledger with the session live. minor: scoped_authority is a general Authority factory (could take session_id and derive the actor); bare WritePermit literal in test_session_writer.py (global-constraint breach, plan-mandated); identity attributes mutable; assert-based None narrowing; unused tmp_path param.
Ruling: hold the session lock for the whole scoped act — `_act` acquires the session lock, then the raw root lock, checks currency, commits, appends the act line, and releases; the _record_act re-check becomes an unreachable invariant (ScienceError). Lock order session → root is consistent (claims/closes take only the session lock; nothing takes the root lock then the session lock). Recorded as design §13 item 18. The bare WritePermit literal is a constraint breach, not polish: fix via narrowed(...).permit. — cost if wrong: a concurrent claim blocks for the duration of an act, which the attended dispatcher already serializes.
Task 7: fix round 1/5 (2 addressed, 0 open — session RLock held across the act, race test, §13 item 18; narrowed(...).permit; commits 1a49a38..dbe9180)
Task 7: complete (commits 9c75761..dbe9180, review clean)
T8 dispatched (implementer opus), BASE dbe9180
T8 implementer DONE_WITH_CONCERNS at c73c203 (3636 passed; hex fixture values; corpus-less findings sort last via sentinel; §6 malformed-chain+act ambiguity kept as act-unverified); reviewer (opus) dispatched
Task 8: review (opus): 3 Important (plan-mandated): (1) reconcile_sessions re-sorts by (code, ref, detail), discarding §6's corpus/position/code order; (2) the sessions/ directory listing runs outside the root lock → a session opened in the window yields a false session-unknown error; (3) session-act-unverified fires for acts naming a corpus whose view is Absent/Malformed or not in `chains` (unadopted), where §6 says nothing is classified. minor (deferred): reader.acts() rebuilt per intent; sort key not total across sessions (add session_id); MalformedView subject dropped from detail; exclude of a live session mis-classifies its intents (unreachable from open); LogEvidenceRefused aborts the whole multi-root audit; coverage gaps (pending clause 2, two sessions one corpus, empty ledger set, torn+open, malformed-chain+act); codes()[0] pins the sentinel order; 15 restated key/finding pairs.
Ruling: all three stand against the plan text with §6 as authority — (1) reconcile_sessions returns extras (sorted-root order) followed by reconcile's own order, no re-sort; (2) the directory listing moves inside the lock; (3) act-unverified restricted to acts whose corpus has a WellFormedView in `chains` — cost if wrong: finding order/coverage differences visible only to the audit reader; J8 arms in Task 10 will exercise them.
Task 8: fix round 1/5 (3 addressed, 0 open — no re-sort, listing under the lock, act-unverified only against WellFormedView; commits c73c203..ad7dfd0)
Task 8: complete (commits dbe9180..ad7dfd0, review clean)
T9 dispatched (implementer opus), BASE ad7dfd0
T9 implementer DONE_WITH_CONCERNS at f8eb249 (acceptance 9 passed; full 3639 passed). Concern 5 verified by controller: tests/acceptance/test_durable_families.py::test_import_on_an_uncertified_tuple_refuses passes on main (779fa7a) and fails on the branch — (None, None) != (None, 0). Cause: _SettlingHold → recover → read_lifecycle_state raises atoms CapabilityUnavailable on the uncertified tmpfs tuple → ExecutionError(applied=None) before the engine's own refusal (applied=0). A Task 3 regression surfaced by Task 9.
Ruling: recover treats CapabilityUnavailable from the lifecycle read exactly like a non-writable state — return and let the write itself refuse (the frozen (None, 0) shape with the CapabilityUnavailable cause); every other lifecycle-read exception still maps to ExecutionError(applied=None). Recorded as §13 item 19. Fix routed to the Task 3 implementer as Task 3 fix round 1 (late), concurrent with Task 9's read-only review — cost if wrong: an uncertified root's write refuses at the engine rather than at settlement, which is the pre-branch behavior.
Task 9 concerns 1–4 noted: J1 unresolved-root refusal repeat and J5 negative belong to Task 10; J1 "malformed under full permit" runs under the full RequiredCapabilities set (refusal still the body's); J3 scoped-time half discharged portably.
Task 9: review (opus): 4 Important: (1) J5 reads act lines from the in-memory index, not the ledger; (2) J11 same for "no act under B"/"every act ledgers under D"; (3) J1 malformed-record arm uses pytest.raises(WriteRefused), which PermitExceeded satisfies; (4) J9 world_id truthiness only and no permit-summary assertion. Plan-mandated (5): J1's unresolved-root refusal repeat absent — carried to Task 10. minor (deferred): delete registration `final` unasserted; coordination arm skips qualify/completion; fsync membership not count; actor shape unpinned; lock sampled once after commit; unused helpers staged for T10; inventory defined after use; J11 second writer not narrower; adopted() bypasses durable_root.
Ruling: findings 1–4 enter the fix loop; finding 5 is placement, not omission — Task 10's dispatch carries it as a required arm (J1 refusal on an unresolved root) — cost if wrong: J1 partially run until Task 10 lands, which the ledger records.
Sequencing: Task 9 fix dispatch waits for the Task 3 late fix to commit (one writer at a time in the worktree).
Task 3: late fix round 1 committed 8994575 (recover defers on CapabilityUnavailable; §13 item 19); implementer used git stash for RED/GREEN — stash list checked empty afterwards. Re-review (sonnet) + Task 9 fix round 1 dispatched concurrently (re-review read-only).
Task 3: late fix round 1/5 (1 addressed, 0 open — recover defers on CapabilityUnavailable, unit test, §13 item 19; commit 8994575). Task 3 remains complete. minor (deferred): redundant local CapabilityUnavailable import in the new test.
Task 9: fix round 1/5 (4 addressed, 0 open — ledger read-back in J5/J11, exact-type body refusal in J1, exact world id + permit summary in J9; commits 8994575..663a2db)
Task 9: complete (commits ad7dfd0..663a2db incl. the Task 3 late fix 8994575, review clean)
T10 dispatched (implementer opus), BASE 663a2db — carries: J1 refusal-on-unresolved-root arm (required), J5 negative = J8 interleaving.
T10 implementer DONE_WITH_CONCERNS at d6c031d (acceptance 24 passed; full 3640; J1 carried arm test_j1_a_refusal_on_a_root_left_unresolved_moves_the_head_only_by_settlement; §13 item 4 amended in place; several brief-vs-engine reconciliations). Reviewer (opus) dispatched.
Task 10: review (opus): 1 Important (plan-mandated): the ledger-fault arm faults os.fsync after the bytes are written, so the act line is present and the registration covered — the inverse of J2's "no act line, one session-outcome-unknown on the registration digest". minor (deferred): "view not rebuilt" observed via the flag only; continuation refusal not discriminating; staged_gone lacks a before-picture; append_intent negative stops at the tip; one-hold arm can pass vacuously if the writer never contended; bare assert in session_faults publish path; guard arm pins the count not the identity. ⚠️ J5 Negative (fabricated entry → act-unverified) and J8 Negatives live in the portable suite (test_session_reconcile.py) — Task 11 may cite them there.
Ruling: the Important stands (J2's text is explicit); fix = a second arm faulting LedgerWriter._write so no bytes land, keeping the fsync arm as the complete-line shape. The one-hold vacuity (minor 5) is promoted into the same round: a cited check that can pass with the writer idle proves nothing — cost if wrong: one extra assertion. §13 item 4's in-place paragraph accepted (accurate, dated, §7/§9 untouched).
T10 fix round 1 committed 88763ca (new arm test_j2_a_ledger_write_failure_after_commit_leaves_the_registration_uncovered; fsync arm retitled test_j2_a_ledger_fsync_failure_after_a_complete_act_line_ends_the_session; one-hold vacuity closed). Re-review (sonnet) + T11 implementer (opus, BASE 88763ca) dispatched concurrently — T11 creates new files only.
Task 10: fix round 1/5 (2 addressed, 0 open — uncovered-registration ledger-write arm added, fsync arm retitled, one-hold vacuity closed; commits d6c031d..88763ca)
Task 10: complete (commits 663a2db..88763ca, review clean)
T11 implementer DONE_WITH_CONCERNS at 8723fac (43 arms/11 units all sound; runner phases 2–3 green; full 3640). Concern 1 verified by controller: cut 17/18 freeze-ancestry pin tests fail on main 779fa7a too (a0f2302/398491d/2071be0/e9e592a unreachable after a history rewrite) — pre-existing, hidden by addopts --ignore=tests/acceptance.
Ruling: out of this lane's scope (frozen cut records of other lanes; a re-pin is their dated amendment). Filed as a task record for main (chore commit after 8723fac). Task 12's results record must state: runner phase 1 stops at cut 17's ancestry pin on main and branch alike; every cut 17/18 arm audit passes; cut 19's own two phases green; classification stays honest about the unrun prefix completion (no "full" by argument). — cost if wrong: cut 19 recorded as partial where a re-pin would make it full; the re-pin is one task away.
Rulings on T11 substitutions: J-inv (sabotage of a test file) not constructible by the harness → J1h renames the seam so the inventory closure fails — accepted; J8f re-cut to drop the lock acquisition — accepted; 15 substitutions itemized in task-11-report.md for the reviewer.
Ruling (Task 12 pre-dispatch): the brief's Step 2 "runner must exit 0 after the cut 18 prefix" cannot hold on this tree or on main (beliefs-faf658). The results record states: phase 1 stops at test_n2_cut17's ancestry pin, failing identically on main; every cut 17/18 N2 arm audit passes when run scoped (13 passed, 2 pin tests failed); cut 19's own phases green (25 acceptance, 7 audit, 43 arms/11 units sound). No claim that the runner exited 0. The record cites beliefs-faf658 as the re-pin the prefix needs. — cost if wrong: the discharge is read as complete where a stricter reading wants the prefix re-run after the re-pin; the record names exactly what to re-run.
Ruling (Task 12 pre-dispatch): the merge into main (brief Step 5's last paragraph) is an outward side effect; Task 12 stops after its branch commit and the merge is presented to the user in the final report — cost if wrong: one extra manual step for the user.
Task 11: review clean (opus). minor (deferred → results record §3): J1h homes a §5-item-4 boundary invariant under J1 (no twelfth unit; a WRITE_ENTRY_POINTS row deletion is not sabotageable by the harness); J8f as landed proves a hold exists across the reads, not that the ledger read is inside it (gap in Task 10's one-hold test); report says 36 ids, table has 38.
Task 11: complete (commits 88763ca..8723fac (+af99233 chore), review clean)
T12 dispatched (implementer opus), BASE af99233
T12 implementer DONE_WITH_CONCERNS at ab386e1 (3640 passed; guards clean; no merge). T12 reviewer (opus) + final whole-branch reviewer (opus) dispatched concurrently, both read-only.
Task 12: review (opus): 1 Important: ledger Current state still says "36-definition inventory" (now 37). minor (deferred): roadmap lane prose "second open lane"/"two kernel lanes" half-updated; §3 lacks cut-18's per-item bold dates; results record homes the race test under J5 in §2 and calls it J7's in §3; runner transcript's `$ echo $?` reconstructed; one 114-col line.
Ruling: Task 12's Important and the lane-prose minor fold into the single final fix wave (one writer; the final whole-branch review is in flight) — cost if wrong: none; both are doc edits.
Final whole-branch review (opus): ready after fixes. Important: (1) ledger reader silently accepts a second session-open (ruling on Task 6 not applied uniformly); (2) `_encode_operation_intent(kind, event_token, actor)` has an `actor` parameter on a new corpus.py definition (Global Constraint), unrecorded. Triage: fix before merge = 1, 2, docstring drift (7); cheap now = _log_seam dedupe (6); roadmap tier-1 numbering skips 4 (16). Minors deferred: _plan_kinds evaluated before require (unreachable through the twins); seam does not validate returned digests; _registration_for full-chain read per write; private return type on public factory; getattr recover probe; KernelRefusalValue duck-typed; close() leaks handle on terminal path; orphan session dir on unspecified open failure; uncommented best-effort close; ten >120-col lines (E501 not enabled); InvocationRecord rebuilt thrice.
Ruling: one fix wave (fresh sonnet implementer) carrying final Important 1, 2, minors 6, 7, 13, 16 and Task 12's ledger "36→37" plus its lane-prose minor; everything else stays deferred and is listed in the final report — cost if wrong: none of the deferred items changes landed behavior on the production port.
Final fix wave committed 13baed4 (fixer stalled on a killed suite run; controller ran gates: 3642 passed, ruff/pyright/probe/guide/tasks clean). Scoped re-review (sonnet) dispatched.
Final fix wave: re-review clean (8/8 addressed, no breakage). Branch ready at 13baed4; merge presented to the user.
