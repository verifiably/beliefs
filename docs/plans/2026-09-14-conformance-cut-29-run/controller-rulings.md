# Cut 29 — controller rulings

These decisions were made while implementing the reviewed plan, in execution
order. The completed checks and preserved diagnostics are in the
[results record](../2026-09-14-conformance-cut-29-results.md).

1. **Combine Task 5's repeated `-k` selectors.** Pytest honors only the last
   selector; one combined expression exercises every intended RED case.
   Cost if wrong: an intended refusal could lack its RED check.
2. **Use certified scratch in the main checkout.** Direct measurement found
   different ext4 feature masks on the worktree volume. Existing root overrides
   select the certified tuple without changing the allowlist or waiver rules.
   Cost if wrong: the durable evidence would not establish certification.
3. **Refresh raw-write fixture readers.** Reopening, with the existing test
   harness root-cache reset, exposes intentionally corrupt records to move and
   consolidation. Cost if wrong: a missing-record refusal could mask the
   address-disagreement check.
4. **Complete Tasks 8 and 9 atomically.** Results, ledger and roadmap guards
   require their updates in one commit. Cost if wrong: inconsistent accounting
   or an invalid intermediate commit.
5. **Adapt cut 20 F8 at its live boundary.** The builder migration displaced its
   frozen source matcher. Keep the canonical declaration unchanged and adapt
   only the live sabotage target. Cost if wrong: the arm could test another
   boundary; baseline, mutation and frozen guards passed.
6. **Keep the entire live caller migration in Task 3.** Its explicit migration
   steps include the durable W3 probe and world-selection references despite
   stale parenthetical ownership. Cost if wrong: task attribution changes;
   complete caller coverage remains required either way.
7. **Test collisions with the original declaration.** A second ordinary add of
   identical bytes must collide; changed bytes derive another address. A forged
   old address is covered separately as disagreement. Cost if wrong: the test
   could confuse collision and admission semantics.
8. **Qualify cut 25 W1-a's live matcher.** The new dataset guard made the old
   generic comparison ambiguous. Cost if wrong: source-address protection could
   lose its mutation oracle; the full cut and frozen guards passed.
9. **Carry only that W1-a normalization through cuts 26–28.** Preserve every
   other arm field and pin. Cost if wrong: overly broad normalization could
   hide declaration drift; scoped review checked the substitution.
10. **Repair decoded lineage ordering at snapshot construction.** The certified
    chain exposed a real mismatch between stored canonical order and decoded
    Route order. Reuse the existing value-sort key; preserve stored bytes,
    validators and traversal positions. Cost: expanded source scope and fresh
    full gates; changing stored order instead would alter on-disk semantics.
11. **Raw-write the intentionally corrupt cut 28 third record.** Ordinary
    admission now correctly refuses its forged address. Admit the valid twins
    normally and preserve the corruption assertions. Cost if wrong: the fixture
    could stop exercising precedence; focused checks and the complete certified
    chain passed after the correction.
12. **Use one final test/documentation fix wave.** Make reproduction instructions
    self-contained, preserve reports, strengthen oracles and add the direct
    replacement-preflight regression. Keep earlier full-run evidence labeled
    accurately and supplement it with 28 focused passes; main's full gate covers
    the added portable test. Cost if wrong: incomplete test coverage, checked
    by the scoped review and merged-main gate.
13. **Accept one residual documentation finding.** The scoped final review found
    obsolete ancestor handles in `python/tests/test_deletion_rows.py:443–444`'s
    docstring; the fixture itself uses derived addresses. The implementation
    workflow permits one final fix wave, so this minor remains explicitly
    recorded. Cost if wrong: misleading test documentation; behavior and its
    assertions are unaffected. No correctness or evidence finding remains open.
